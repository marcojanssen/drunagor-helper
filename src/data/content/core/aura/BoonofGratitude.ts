import type { AuraData } from "@/data/repository/AuraData";

export class BoonofGratitude implements AuraData {
  id = "boon-of-gratitude";
  name = "Boon of gratitude";
  effect = "PREVENTS gain a +1 bonus";
}
